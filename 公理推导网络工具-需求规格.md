# 数学公理推导网络工具 — 需求规格

## 1. 背景与目标

用于记录从少数初始公理出发,逐步推导出大量定理、推论,并形成交叉引用网络的学习过程。核心痛点:传统线性笔记(包括 Markdown 双链)无法直观呈现"多分支交叉推导"的结构,导致难记录、难回看。

工具目标是**面向人类可读性和学习理解**,不是面向机器证明验证——不追求形式化、不需要跟踪"已证明/未证明"状态,而是要让使用者能随时看清"这条定理是怎么一步步从哪些前提推出来的"。

长期使用工具,数据需持久化保存,支持随时增量添加,不要求一次性整理完成。

---

## 2. 数据模型

每个节点代表一条命题,包含:

- `id`:唯一标识
- `type`:公理 / 定义 / 定理 / 推论(用于视觉区分)
- `title`:简短名称
- `statement`:命题的自然语言陈述
- `proof_sketch`:证明思路的一两句概括(不要求完整形式化证明)
- `note`:使用者自己的批注(可选,比如"这一步的关键技巧是XX")
- `full_proof`:完整证明正文(可选,默认折叠隐藏,由使用者自行编辑填写;工具不做自动验证,纯文本记录用)
- `depends_on`:该命题直接依赖的前提节点列表(上游)

`depends_on` 之外不需要用户手动维护"被依赖"关系——下游节点(谁用到了我)由系统根据全部节点的 `depends_on` 反向自动计算得出,避免双向手动同步的麻烦。

---

## 3. 关系建立方式(不打字建关系)

命题正文(`statement`、`proof_sketch`、`note`)仍需打字输入,这部分无法避免。但**结构关系的建立必须是点选/拖拽,不能靠手写引用语法**:

- 方式一:在图上从节点 A 拖一条线到节点 B,即建立"B 依赖 A"
- 方式二:编辑节点 B 时,从一个可搜索的节点列表中勾选前提节点,而不是在文本里手写 `[[A]]` 这种链接

两种方式应同时支持,分别适合"边看图边连线"和"编辑单个节点时批量勾选前提"两种场景。

---

## 4. 图谱布局:保留两种方案

由于屏幕(尤其手机屏幕)不可能一次性完整显示全图,不预设单一布局方案,两种布局都要实现,由用户手动切换:

- **分层布局**:按"距公理的推导层数"排列,公理在起点,层数越深越往外延伸,适合看清推导深度和链路顺序。
- **力导向布局**:节点按关联紧密程度自动聚拢,不强制分层,适合看清哪些命题/分支联系紧密。

两种布局都需要配合**缩放、平移**,以及一个"聚焦模式"作为默认视图:点击某个节点后,只清晰展示该节点及其直接上下游(前提链 + 被引用链),其余节点变灰或隐藏,避免一次性铺开全图导致的视觉过载。全图视图可以作为一个可选的缩略导航(小地图),而不是默认的主视图。

---

## 5. 关于连线交叉

推导关系是有向图,不是所有情况都能画成不交叉的连线(这是图的结构决定的,不是布局没做好)。因此:

- 连线交叉本身**不需要**任何特殊语义标记或高亮处理,按普通边正常绘制即可
- 布局算法可以尽量减少不必要的交叉以提升可读性,但不必也不可能消除所有交叉
- 真正解决"看不清"问题的手段是第 4 条里的聚焦模式,而不是消除交叉线本身

---

## 6. 视觉设计要点

- 节点按 `type` 用颜色或形状区分(公理 / 定义 / 定理 / 推论)
- 点击节点展开详情面板,显示 `statement`、`proof_sketch`、`note`;`full_proof` 默认折叠为一个可展开的区块,展开后可编辑,不展开不影响图面简洁度
- 选中节点时,高亮其完整的上游前提链和下游推论链,其余部分弱化(变灰/半透明)
- 需要按名称/类型搜索定位节点的功能,图谱规模变大后仅靠视觉浏览会不够用

---

## 7. 开发协作方式

使用者是编程新手,当前阶段只想学习 Git 操作,不学习编写代码本身(代码能力的培养推迟到以后)。因此:

- 全部代码由 agent 编写完成,不需要把任何编码任务分给使用者
- 每完成一个可独立验证的功能点,就带使用者做一次真实的 Git 提交(如 `git add`、`git commit`、`git push`、创建分支、发起/合并 PR 等),边操作边讲清楚"这次改动是什么、为什么单独拆成一次提交",让 Git 的学习依附在真实的项目进展上,而不是脱离项目单独刷 Git 教程
- 提交粒度建议按功能拆分得细一些(而不是攒成几个大提交),这样使用者能通过提交历史直观看到项目是怎么一步步搭起来的,也有更多次数练习 Git 操作

## 9. 当前版本非目标(用于控制首版范围,非永久排除)

以下内容不在首个版本的实现范围内,是为了让开发聚焦、避免范围蔓延,但后续版本有扩展的可能,开发时预留相应的架构空间(不代表要现在就设计好接口):

- 多人协作/教学演示功能
- 与 Obsidian/Notion 等现有笔记软件集成
- 形式化证明验证,或与 Lean/Coq 等证明助手对接(注意:`full_proof` 字段只是给使用者自己记录完整证明文本的地方,不涉及自动验证,与此条非目标不冲突)
- "证明完成状态"跟踪(这是形式化工具的关注点,目前不是本工具目标)

---

## 10. 可扩展性与自定义空间(为生态预留,不要求首版实现)

架构上预留以下几个扩展点,方便以后用户自定义或形成社区生态,但首版不需要真的做出插件市场之类的东西:

- **节点类型可扩展**:公理/定义/定理/推论只是默认的四种类型,数据结构上不要写死,允许用户自定义新类型(比如"猜想""反例"),并自定义对应的颜色/图形。
- **数据导入导出用开放格式**:整个知识网络能导出为一个结构清晰的 JSON(或类似)文件,字段对应第 2 节的数据模型。这样即使以后想换工具、写脚本批量处理、或者和别人交换自己整理的公理网络,都不会被锁死在这一个工具里。
- **自定义高亮/筛选规则预留接口**:比如"只显示某个类型的节点""按关键词筛选""自定义配色方案"这类规则,架构上作为可插拔的规则模块处理,而不是写死在核心逻辑里。
- **布局算法可替换**:第 4 节的分层/力导向只是内置的两种,底层布局计算和渲染分开,以后要加入第三种布局方式(比如社区做的自定义布局脚本)时不需要改动核心数据模型。

## 11. 待开发方确认的技术选型(本文档不做决定)

- 前端图谱渲染库选型(如 D3.js / Cytoscape.js / vis-network 等,需支持有向图 + 两种布局切换 + 聚焦模式)
- 数据持久化方式(本地文件 / 浏览器本地存储 / 后端数据库),取决于是否需要跨设备访问
- 是否需要移动端专门的手势交互适配(捏合缩放、长按拖拽连线等)
