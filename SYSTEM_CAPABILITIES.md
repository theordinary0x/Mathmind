# MathMind 系统能力与功能全景指南 (SYSTEM_CAPABILITIES.md)

本文档是 MathMind 系统的全局功能资产盘点、快捷键单一标准对照表与未来演进路线图（Backlog）。旨在避免开发过程中遗忘已实现特性，并为后续架构演化提供基准。

---

## 1. 系统核心功能资产盘点 (Implemented Capabilities)

### 1.1 知识图谱画布引擎 (Graph Canvas Engine)
* **双模拓扑布局**：
  * **层次布局 (Dagre)**：严格按数学命题因果依赖（前提 $\to$ 结论）自左向右/自顶向下排列，展现推导层级。
  * **力导向布局 (CoSE)**：模拟物理力学斥力与弹簧引力，适合探索复杂非线性网状关联。
  * **平滑动画与自适应聚焦**：布局切换配有 $500\sim600\text{ ms}$ 的缓动动画（`ease-out-cubic`），并在重排完成后自动平滑居中画幅。
* **增量式差量同步**：在同一项目中新增、删除或修改命题时，画布仅对变动节点进行增量 Diff，绝不全局重刷，保留未修改节点的绝对坐标。
* **高质感多重背景预设**：
  * **纸张 (Paper/Parchment)**：专有数学羊皮纸浅色微纹理。
  * **石板黑板 (Chalkboard)**：细腻深绿微晶质感与微粉笔擦痕。
  * **黑曜石 (Dark/Obsidian)**：深邃黑曜石渐变与细微颗粒。
  * **网格与点阵 (Grid & Dots)**：经典数学工程坐标网格。
  * 支持自定义背景图片、透明度与高斯模糊调节。
* **视口与性能管理**：
  * 彻底接管浏览器原生滚轮事件，消除 Windows 滚轮 Delta 瞬时膨胀与 Cytoscape 阻尼迟滞。
  * 鹰眼导航器（MiniMap）实时预览视口全局。

### 1.2 画布交互与选择系统 (Interactive Selection & Tools)
* **解耦式点击设计**：
  * **单机左键（Single Left-Click）**：**仅做选中**（高亮节点、支持多选与拖拽），绝不触发侵入式弹窗。
  * **双击节点（Double-Click）/ 回车键（Enter）**：打开命题编辑详情弹窗。
  * **双击空白画布（Double-Click Canvas）**：在鼠标光标世界坐标处直接就地创建新命题。
* **多模式选择**：
  * **单选与连续多选**：点击选中单个；按住 `Ctrl`/`Cmd` 点击节点追加/切换选中。
  * **矩形框选（Box Selection - 快捷键 `B`）**：框选区域内批量选定节点。
  * **自由套索（Lasso Selection - 快捷键 `L`）**：多边形自由套索圈选任意非凸几何区域。
  * **批量拖拽**：在多选状态下拖动其中任意一个节点，所有被选节点保持相对位置刚体平移。
  * **悬浮操作岛与批量控制栏**：批量全选、取消选择、一键批量删除。
* **连线模式（Connect Mode - 快捷键 `C`）**：
  * 激活后鼠标变为十字准星，依次点击两节点即可建立依赖关系，内置自环与重复依赖校验。

### 1.3 数学命题体系与渲染规范 (Mathematical Propositions & Rendering)
* **标准数学范畴体系**：
  * 定义（Definition）、公理（Axiom）、引理（Lemma）、定理（Theorem）、推论（Corollary）、猜想（Conjecture）。
  * 严格的色彩标识体系（浅色/深色主题适配的边框与底色）。
* **KaTeX 高精度公式支持**：
  * 支持行内公式 `$...$` 与块级居中公式 `$$...$$`。
  * **画布节点排版折行优化**：Canvas 矢量渲染层利用 `latexToUnicode` 转译常见数学符号并折行排版；弹窗与预览区利用 KaTeX 原生排版。
* **命题详情双模体系（阅读模式 / 编辑模式）**：
  * **默认阅读模式 (Read Mode)**：打开已有命题时默认以出版级纯净排版展开，隐藏所有输入框边框与 LaTeX 符号条；陈述、推导思路与证明以 KaTeX 原生公式自然排版；前置与下游依赖为可点击徽章，点击直接在弹窗内穿梭阅读关联命题。
  * **便捷编辑模式 (Edit Mode)**：支持按 `E` 键或顶部切换按钮秒切进入编辑状态，配备 LaTeX 快捷符号插入条与实时预览。
* **聚焦模式（Focus Mode - 快捷键 `F`）**：
  * 聚焦选中命题的完整上游推导链（Predecessors）与下游结论链（Successors），高亮推导血缘，非相关节点自动半透明淡化。

### 1.4 AI Copilot 智能助教与教材解析 (AI Copilot & Ingestion)
* **无缝侧边栏与边缘色彩一致性**：
  * 侧边栏与主界面 Header 采用纯平无缝衔接设计（`#18181B` 深色 / `#FAF8F5` 纸张色），彻底杜绝色差拼缝与圆角违和感。
* **多轮智能对话与上下文感知**：
  * 自动将当前选中的命题及其直接上下游依赖注入 AI 提示词上下文。
  * 公式自动渲染，命题高亮引用与点击直达。
* **AI 证明漏洞检查与定理推荐**：
  * 自动审视所选定理证明链中缺失的前提或逻辑跳跃。
* **教材智能解析与 Diff 变更提案审查**：
  * 丢弃笨重的单次提取弹窗，升级为 Copilot 内置文本/讲义解析。
  * 生成结构化 `DiffReviewCard`，支持在图谱上进行变更提案预览、逐条勾选应用或一键合并入图谱。

### 1.5 项目管理与数据持久化 (Projects & Persistence)
* **多项目自由管理（Project Manager - 快捷键 `P`）**：创建新项目、重命名、切换项目、删除项目。
* **工程级 JSON 导入导出**：支持项目另存为单一 JSON 讲义文件或全项目打包导出。
* **三档自动保存模式**：
  * **实时保存 (Immediate)**：任何节点或连线变动后立即持久化。
  * **定时保存 (Interval)**：可设置 10s、30s、1min、5min 定时保存。
  * **手动保存 (Manual)**：完全由用户手动触发（`Ctrl+S`）。
* **历史状态回溯**：轻量化防抖历史记录栈（`Ctrl+Z` 撤销，`Ctrl+Y` 重做）。

---

## 2. 单一快捷键标准速查表 (Single 1-to-1 Keyboard Shortcuts)

> [!IMPORTANT]
> 系统已全面收敛并剔除所有重复别名（如曾存在的 `N` 与 `Ctrl+N`、`P` 与 `Ctrl+P` 等多重映射），全面推行严谨单一标准。
> 输入法合成中（IME 状态）或光标位于输入框内时，单键快捷键自动挂起，防止文字录入冲突。

| 功能分类 | 快捷键 | 行为说明 |
| :--- | :---: | :--- |
| **项目与系统** | `P` | 打开/关闭项目管理面板 |
| | `Ctrl+,` | 打开系统设置面板 |
| | `Ctrl+S` | 立即保存当前工程 |
| | `Ctrl+Shift+S` | 另存为独立 JSON 文件 |
| | `T` | 循环切换界面外观模式（深色 / 浅色） |
| | `?` | 打开本快捷键对照表 |
| **画布视口** | `0` | 重置缩放并适应全图（Fit Canvas） |
| | `=` | 放大画布视口 |
| | `-` | 缩小画布视口 |
| | `/` | 聚焦全局命题搜索栏 |
| | `Tab` | 切换布局算法（层次布局 Dagre $\leftrightarrow$ 力导向布局 CoSE） |
| **命题操作** | `N` | 新建命题（打开创建弹窗） |
| | `Enter` | 打开当前选中命题的详情弹窗 |
| | `E` | 在命题详情弹窗内切换阅读 / 编辑模式 |
| | `Delete` | 删除选中的单个或批量命题 |
| | `Ctrl+C` | 复制选中命题 |
| | `Ctrl+V` | 粘贴命题至画布 |
| | `F` | 开启/关闭当前选中命题的拓扑推导聚焦链 |
| | `Esc` | 清空所有选中项并退出当前工具模式 |
| **交互模式** | `B` | 开启/关闭矩形框选模式（Box Selection） |
| | `L` | 开启/关闭自由套索模式（Lasso Selection） |
| | `C` | 开启/关闭手动连线模式（Connect Mode） |
| | `I` | 打开/折叠 AI Copilot 侧边栏 |
| **历史栈** | `Ctrl+Z` | 撤销上一步操作 |
| | `Ctrl+Y` | 重做已撤销操作 |

---

## 3. 待办需求与未来演进路线图 (Future Backlog)

以下为已确认在后续版本中演进的高价值能力，现阶段优先保障架构健壮性与代码整洁度：

- [ ] **SVG 矢量格式与 4K 超清截图导出**：在现有 Canvas PNG 导出基础上，引入 Cytoscape-SVG 插件，输出无损数学出版级矢量图。
- [ ] **拓扑讲义线性导出 (Topological Markdown / PDF Export)**：利用图谱的拓扑排序算法（Kahn / DFS），自动将网状命题依赖解构为自洽的线性教科书讲义，导出为 Markdown 或 LaTeX/PDF 文档。
- [ ] **环路实时检测与视觉警告 (Cycle Detection & Visual Warning)**：连线时不仅阻止成环，同时为意外产生的逻辑死循环（$A \implies B \implies A$）提供显眼的红色呼吸光效与告警面板。
- [ ] **推导链最短路径寻径 (Shortest Proof Path Finder)**：用户指定起始已知公理与目标待证定理，自动高亮两者之间的最短逻辑推导链。
- [ ] **命题标签与分支学科过滤 (Tagging & Discipline Filtering)**：支持按代数、分析、几何等学科标签进行视图过滤。
