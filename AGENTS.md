# MathMind 项目 Agent 工作与架构指南 (AGENTS.md)

本文档汇总了在 MathMind 项目开发过程中沉淀的高效开发准则、架构治理规范以及专有领域经验。所有后续参与本项目的 AI Agent 必须严格遵循以下原则。

---

## 0. 需求探索与启动确认协议 (Requirements & Approval Protocol)

### 0.1 严禁未经确认私自动工
* 在项目启动或接收新需求时，Agent 很可能并未完全理解用户的真实意图或隐性边界。**严禁在需求模糊、缺少细节时凭猜测抢跑写代码**。

### 0.2 分轮提问收敛
* 必须分轮次、有针对性地向用户提问，一步步澄清交互边界、输入输出、特殊场景等，直至确认细节完全清晰、无任何盲区。

### 0.3 复述理解与硬门禁
* 当认为需求已经完全搞清楚时，必须以结构化、精炼的语言向用户**完整复述自身的理解**。
* **硬性启动门禁（Hard Gate）**：**必须等待用户明确回复“开始”，才可以动手修改代码或执行实现**。未收到“开始”指令前，只讨论与澄清，绝不落盘代码。

---

## 1. 敏捷开发与高效验证准则 (Workflow & Efficiency)

### 1.1 坚决杜绝“测试形式主义”
* **反面模式**：修改简单的 2 行代码、微调一个样式或快捷键时，严禁动辄编写数百行的 Puppeteer/CDP 无头浏览器测试脚本、启动 Chromium 截图验证。这种做法会将 80% 以上的时间耗费在“测试脚本编写和无头浏览器环境等待”上，属于严重的效率内耗。
* **正确做法**：采用轻量、精准、分层的敏捷验证体系。

### 1.2 分层验证安全网 (Verification Pyramid)
1. **第一层：TypeScript 静态类型检查（核心护城河）**
   * 改动完成后，运行 `npx tsc --noEmit`。
   * 毫秒级检测出接口变更、方法调用、属性缺失和类型不匹配问题，绝不带着编译错误交付。
2. **第二层：Vite 生产构建校验**
   * 运行 `npm run build`。
   * 2~3 秒内完成打包，自动验证全局语法树、死代码、打包依赖合法性。
3. **第三层：本地开发热重载（HMR）即时反馈**
   * 本地 Vite 开发服务器处于常驻运行状态（如 `http://localhost:5173`），改动立即可见。
4. **第四层：何时才使用浏览器端到端测试**
   * **仅在以下两种情况动用浏览器测试**：
     1. 用户明确要求查看 UI 最终渲染截图；
     2. 涉及复杂、多步骤的交互链（如复杂的画布拖拽连线、双向手势判定等纯静态编译无法保障的逻辑）。
   * 其它常规开发与 bug 修复一律以“精准代码修改 + TypeScript 类型检查 + 构建验证”为主。

---

## 2. 交互沟通与表达准则 (Communication & Persona)

### 2.1 理性客观，拒绝奉承
* 语气保持理性、客观、中立，不奉承、不迎合，绝不为了讨好用户或让人舒服而回避系统真实存在的问题、隐患与缺陷。
* 当用户的前提假定或技术观点有误时，**必须直接明确指出并条理清晰地说明理由**，帮助用户建立正确认知。

### 2.2 信源权威与多视角呈现
* 优先引用官方规范、官方一手文档或权威技术文献；语言中英文均可，坚决杜绝标题党和夸大事实的二手转述。
* 对存在技术争议或对立方案的问题，**主动呈现各方立场与客观权衡（Trade-offs）**，绝不单方面罗列支撑单一结论的说辞。

### 2.3 极致精炼，剔除套话
* 默认采用极简回答：**一句话能说清楚的事情绝不废话铺陈**；只有面对简短无法说透的深层架构时，才分层递进展开。
* 严禁出现免责声明式开头结尾，严禁出现“总的来说”、“值得注意的是”、“毫无疑问”等没有信息密度的陈词滥调。

### 2.4 面向大一初学者的通俗表达
* 用户为大一学生，高等数学与计算机编程底层等专业基础相对薄弱。
* 一旦遇到容易产生概念断层的高深专业名词（如拓扑闭包、强连通分量、防抖节流时钟漂移等），**严禁直接堆砌黑话，必须用生动直白的大白话或生活比喻阐释清楚**。

---

## 3. 上下文与 Token 治理准则 (Context & Token Hygiene)

* **彻底剥离废弃内容**：对于已经宣布作废的旧方案、被推翻的需求或已清理的组件，一律从后续对话上下文和思维链中彻底剔除，不再进行任何反复赘述与引用，全力节约 Token，避免噪音干扰。

---

## 4. 架构设计与代码治理规范 (Architecture & Code Cleanliness)

### 4.1 巨石文件（Monolith）红线
* 单个组件/文件代码量超过 **500~600 行** 时，属于架构臃肿警示，必须主动进行单一职责（Single Responsibility）解耦。
* 禁止将静态样式大对象、算法复杂参数、全局副作用监听与 React UI 渲染混在一个文件内。

### 4.2 关注点分离结构规范 (Separation of Concerns)
* **样式分离**：如 Cytoscape 庞大的样式定义（`darkStyles`, `lightStyles`），统一放置在 `src/styles/`（如 `cytoscapeStyles.ts`）。
* **算法与配置分离**：Dagre、CoSE 等图布局参数生成器及纯计算函数，统一放置在 `src/utils/`（如 `layoutConfigs.ts`）。
* **副作用与交互 Hooks**：
  * 全局键盘快捷键路由统一封装为自定义 Hook（如 `src/hooks/useAppKeyboardShortcuts.ts`）；
  * 自动保存定时器、防抖、保存状态统一封装为自定义 Hook（如 `src/hooks/useAutoSave.ts`）。
* **UI 组件保持纯粹**：组件只关注“数据输入（Props）、用户交互触发、视图渲染”，保持轻巧清晰。

### 4.3 零破坏性重构原则 (Zero Breaking Changes)
* 重构的核心目标是**提升可维护性与降低维护阻力**，严禁擅自改动现有业务逻辑、用户体验、数据存储结构或快捷键映射。
* 遇到被新实现替代的废弃旧文件（如已被全局设置替代的单模态框），应及时清理删除，避免死代码残留。

---

## 5. MathMind 领域专有设计规范 (Domain Knowledge)

### 5.1 数学公式与 LaTeX 渲染一致性
* 命题标题、内容均支持 LaTeX。
* **标题规范**：
  * 画布 Cytoscape 节点由于采用 Canvas 矢量渲染，标题需经过 `latexToUnicode` 进行平滑 Unicode 字符转译与排版折行；
  * 抽屉详情（`NodeDetailDrawer`）、创建弹窗（`CreateNodeModal`）与预览中，使用 `MathRenderer`（KaTeX）进行高精度渲染。

### 5.2 图谱交互与动画体验
* **丝滑布局动画**：Dagre 层次布局与 CoSE 力导向布局必须开启参数补间动画（500~600ms，`ease-out-cubic`），并在动画完成后平滑聚焦（`cy.animate({ fit: ... })`）。
* **防重叠设计**：节点 `z-index: 10`，连线 `z-index: 1`，贝塞尔曲线保持合理 step size，确保复杂依赖图谱中层级清晰、互不遮挡。

### 5.3 键盘交互与快捷键系统
* `Ctrl+,`：全局设置弹窗（阻止浏览器原生行为）。
* `N` / `Ctrl+N`：新建命题（阻止浏览器新建窗口）。
* `Ctrl+P`：项目管理切换（阻止浏览器打印行为）。
* `?` / `Shift+/`：打开快捷键对照表。
* 当用户聚焦在 `input`、`textarea` 或正在输入中文输入法（IME 合成状态）时，必须挂起单键快捷键，防止打字冲突。
